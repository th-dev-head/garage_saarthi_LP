import React from "react";
import PromoCTA from "../../common/PromoCTA";

export default function MGFinalCTA() {
  return (
    <PromoCTA
      title="Start Documenting Repairs Visually Today"
      subtitle="Create jobs, attach scratch check-ins, record component photo wear logs, and share everything instantly on client messaging streams."
    />
  );
}
